# ========================================
# WAITLIST WIDGET - ENVIRONMENT VARIABLES
# ========================================
# 1. Copy this file: cp env.example .env.local
# 2. Fill in REQUIRED values below
# 3. Upload .env.local to Vercel dashboard
# Docs: docs/QUICKSTART.md

# ----------------------------------------
# REQUIRED - Fill these in!
# ----------------------------------------

# Supabase (https://supabase.com/dashboard → Settings → API)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_KEY=your-service-role-key

# Resend (https://resend.com/api-keys)
RESEND_API_KEY=re_xxxxx
FROM_EMAIL="Your Project <hello@yourdomain.com>"

# Your website URL (where confirmation pages live)
BASE_URL=https://yourdomain.com

# API URL (where API endpoints are deployed - defaults to BASE_URL if not set)
# Use this if your API is on a different domain than your main website
# Example: API_URL=https://api.yourdomain.com or API_URL=https://a.yourdomain.com
API_URL=

# CORS (comma-separated domains allowed to call API)
CORS_ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# ----------------------------------------
# EMAIL BRANDING (optional - can leave as-is or customize)
# ----------------------------------------

# Template style: minimal (default), professional, branded
EMAIL_TEMPLATE_STYLE=minimal

# Branding (can leave as-is - has defaults in templates/config.js)
# Logo and branded text options only apply to the Professional and Brand templates.
EMAIL_PROJECT_NAME="Your Project"
EMAIL_SENDER_NAME="The Team"
EMAIL_PRIMARY_COLOR=#4f46e5
EMAIL_LOGO_URL=https://yourdomain.com/logo.png
EMAIL_BRANDED_TEXT_ONLY=TRUE

# SPAM Compliance (Required for marketing emails)
# Physical postal address - REQUIRED by CAN-SPAM Act for commercial emails
# Can be street address, P.O. Box, or private mailbox
# Example: "123 Main St, City, State 12345" or "P.O. Box 123, City, State 12345"
EMAIL_SENDER_ADDRESS=

# Leave blank if emails are transactional/relationship-based (default waitlist emails are not promotional)
# Only set this if you customize the emails to include promotional content
# Example: "This email is an advertisement."
EMAIL_ADVERTISEMENT_DISCLOSURE=

# GDPR Compliance (Recommended)
# Privacy policy URL - recommended for GDPR compliance
# Will appear in email footers and enable consent checkbox in waitlist form
# Example: EMAIL_PRIVACY_POLICY_URL=https://yourdomain.com/privacy
EMAIL_PRIVACY_POLICY_URL=

# ----------------------------------------
# OPTIONAL FEATURES - Leave blank if not using
# ----------------------------------------

# Resend Contacts sync (https://resend.com/audiences)
# Leave blank if not using Contacts sync
RESEND_AUDIENCE_ID=
RESEND_WEBHOOK_SECRET=

# Redirect root URL to main site (leave blank for local testing page)
MAIN_WEBSITE_URL=

# Bot protection (https://dash.cloudflare.com → Turnstile)
# Leave blank if not using CAPTCHA
TURNSTILE_SECRET_KEY=

# Rate limiting (https://upstash.com)
# Leave blank if not using rate limiting
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Disable double opt-in (leave blank to keep enabled - recommended)
DOUBLE_OPTIN=

# ----------------------------------------
# REDIRECT URLS (can leave as-is - rarely need to change)
# ----------------------------------------

CONFIRM_SUCCESS_URL=/waitlist-confirmed
CONFIRM_ERROR_URL=/waitlist-error
UNSUBSCRIBE_SUCCESS_URL=/unsubscribe-success
UNSUBSCRIBE_ERROR_URL=/unsubscribe-error
